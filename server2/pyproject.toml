[project]
name = "server2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.0",
    "flask-cors>=5.0",
    "redis>=5.0",
    "yt-dlp>=2024.1.1",
]
