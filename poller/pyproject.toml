[project]
name = "poller"
version = "0.1.0"
description = "YouTube livestream poller — writes live/upcoming streams to Redis."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "redis>=5.0",
    "yt-dlp>=2026.03.17",
]

[dependency-groups]
dev = []
